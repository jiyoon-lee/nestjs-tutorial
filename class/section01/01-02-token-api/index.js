function createTokenOfPhone(qqq) {
  // 매개변수(parameter)
  // 1. 휴대폰번호 자릿수 맞는지 확인하기(10~11자리)
  if (qqq.length >= 10) {
    if (qqq.length <= 11) {
    } else {
    }
  } else {
  }

  // 2. 핸드폰 토큰 6자리 만들기

  // 3. 핸드폰번호에 토큰 전송하기
}

createTokenOfPhone("0123456789"); // 인자(argument)
